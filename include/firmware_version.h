#pragma once

// Increment before deploying each changed firmware image, including test builds.
// Keep this the single version source for MQTT, the HMI and the boot log.
#define GDC_FIRMWARE_VERSION "1.1.8"
#define GDC_FIRMWARE_MARKER "GDC-FW:" GDC_FIRMWARE_VERSION
